# Integrations

Detected integrations
- None detected in source files. The project appears to be a standalone Python package.

Potential integrations
- CI: add GitHub Actions or Azure Pipelines for linting and tests
- Packaging: configure PyPI publish workflow using `pyproject.toml` metadata

Next steps
- Add CI config in `.github/workflows/` to run tests and build wheels.
# Integrations

No external third-party service integrations were detected from repository files.

Search hints:
- Look for environment/config files like `.env`, `config/`, or `settings.py`.
- Search code for libraries such as `boto3`, `google`, `requests`, `slack`, or SDKs that indicate external services.

If your project depends on cloud storage or APIs, add an `INTEGRATIONS.md` entry describing required credentials, scopes, and any setup steps.
