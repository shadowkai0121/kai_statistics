---
phase: 01-library-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kai_statistics/__init__.py
  - src/kai_statistics/io.py
  - src/kai_statistics/profiling.py
  - src/kai_statistics/descriptive.py
  - src/kai_statistics/testing.py
autonomous: true
requirements: [INFRA-01, INFRA-04]
user_setup: []
must_haves:
  truths:
    - "User can import kai_statistics as ks and discover explicit module boundaries for io, profiling, descriptive, and testing."
    - "The package root exposes stable, intentional exports instead of relying on implicit module discovery."
  artifacts:
    - path: "src/kai_statistics/__init__.py"
      provides: "Package-root public surface and explicit module exports"
      exports: ["__version__", "descriptive", "io", "profiling", "testing"]
      contains: "__all__"
    - path: "src/kai_statistics/io.py"
      provides: "Importable io boundary for future data-intake helpers"
      contains: "__all__ = []"
    - path: "src/kai_statistics/profiling.py"
      provides: "Importable profiling boundary for future dataset-inspection helpers"
      contains: "__all__ = []"
    - path: "src/kai_statistics/descriptive.py"
      provides: "Importable descriptive boundary for future summary helpers"
      contains: "__all__ = []"
    - path: "src/kai_statistics/testing.py"
      provides: "Importable testing boundary for future inferential helpers"
      contains: "__all__ = []"
  key_links:
    - from: "src/kai_statistics/__init__.py"
      to: "src/kai_statistics/io.py"
      via: "explicit package import"
      pattern: "from \\. import descriptive, io, profiling, testing"
    - from: "src/kai_statistics/__init__.py"
      to: "src/kai_statistics/descriptive.py"
      via: "__all__ export list"
      pattern: "__all__ = \\[\"__version__\", \"descriptive\", \"io\", \"profiling\", \"testing\"\\]"
---

<objective>
Deliver the importable package skeleton that turns the minimal stub into a real library surface for Phase 2 work without pre-creating every future analysis area.

Purpose: Establish a notebook-friendly package entry point and a small set of clear module boundaries that match the roadmap and discuss-phase decisions.
Output: Updated package root plus importable `io`, `profiling`, `descriptive`, and `testing` boundary modules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-library-infrastructure/01-CONTEXT.md
@.planning/phases/01-library-infrastructure/01-PATTERNS.md
@AGENTS.md
@pyproject.toml
@src/kai_statistics/__init__.py
</context>

<threat_model>
<trust_boundaries>
- Package consumers cross from arbitrary notebook code into the public `kai_statistics` import surface.
- The public package root must not promise future helpers that do not exist yet.
- Phase 2 and Phase 3 work will depend on these module names remaining stable.
</trust_boundaries>

<stride_register>
- threat_id: T-01-01
  category: tampering
  component: package-root exports
  disposition: mitigate
  mitigation_plan: Keep exports explicit in `src/kai_statistics/__init__.py`, avoid wildcard imports, and smoke-test the intended module names.
- threat_id: T-01-02
  category: spoofing
  component: future helper boundaries
  disposition: mitigate
  mitigation_plan: Create only the Phase 2-facing boundary modules (`io`, `profiling`, `descriptive`, `testing`) and do not create fake helpers or additional future-domain stubs.
- threat_id: T-01-03
  category: denial_of_service
  component: notebook import path
  disposition: mitigate
  mitigation_plan: Match `__version__` to `pyproject.toml`, keep imports lightweight, and preserve a smoke-safe import path for `import kai_statistics as ks`.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the package root into an explicit boundary surface</name>
  <files>src/kai_statistics/__init__.py</files>
  <read_first>src/kai_statistics/__init__.py, pyproject.toml, .planning/phases/01-library-infrastructure/01-CONTEXT.md, .planning/phases/01-library-infrastructure/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Update `src/kai_statistics/__init__.py` so it becomes the notebook-facing import surface for Phase 1. Set `__version__ = "0.1.0"` to match `pyproject.toml`, import `descriptive`, `io`, `profiling`, and `testing` with the exact statement `from . import descriptive, io, profiling, testing`, and define `__all__ = ["__version__", "descriptive", "io", "profiling", "testing"]`. Keep `_hello()` as a guaranteed smoke-only helper so downstream smoke tests have a stable target, but do not expose fake statistical helpers from the package root and do not create `modeling` or `plotting` stubs in this phase.</action>
  <verify>uv run python -c "import kai_statistics as ks; expected = ['__version__', 'descriptive', 'io', 'profiling', 'testing']; assert ks.__version__ == '0.1.0'; assert ks.__all__ == expected; assert callable(ks._hello); assert all(hasattr(ks, name) for name in expected[1:])"</verify>
  <acceptance_criteria>
    - `src/kai_statistics/__init__.py` contains `__version__ = "0.1.0"`
    - `src/kai_statistics/__init__.py` contains `from . import descriptive, io, profiling, testing`
    - `src/kai_statistics/__init__.py` contains `__all__ = ["__version__", "descriptive", "io", "profiling", "testing"]`
    - `src/kai_statistics/__init__.py` contains `def _hello(`
  </acceptance_criteria>
  <done>Package root exposes the intended boundary modules and imports cleanly via `import kai_statistics as ks`.</done>
</task>

<task type="auto">
  <name>Task 2: Add thin boundary modules for near-term analysis domains</name>
  <files>src/kai_statistics/io.py, src/kai_statistics/profiling.py, src/kai_statistics/descriptive.py, src/kai_statistics/testing.py</files>
  <read_first>src/kai_statistics/__init__.py, .planning/phases/01-library-infrastructure/01-CONTEXT.md, .planning/phases/01-library-infrastructure/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Create `src/kai_statistics/io.py`, `src/kai_statistics/profiling.py`, `src/kai_statistics/descriptive.py`, and `src/kai_statistics/testing.py` as importable boundary modules. Give each file a concise module docstring and the exact line `__all__ = []`. Add a short comment or docstring sentence describing the future responsibility of that module, but keep the files logic-free: no placeholder helper functions, no fake return values, no classes with `pass`, and no extra future-domain stubs beyond these four modules.</action>
  <verify>uv run python -c "import kai_statistics.descriptive as descriptive; import kai_statistics.io as io; import kai_statistics.profiling as profiling; import kai_statistics.testing as testing; assert descriptive.__all__ == []; assert io.__all__ == []; assert profiling.__all__ == []; assert testing.__all__ == []"</verify>
  <acceptance_criteria>
    - `src/kai_statistics/io.py` contains `__all__ = []`
    - `src/kai_statistics/profiling.py` contains `__all__ = []`
    - `src/kai_statistics/descriptive.py` contains `__all__ = []`
    - `src/kai_statistics/testing.py` contains `__all__ = []`
  </acceptance_criteria>
  <done>All four boundary modules import successfully and remain intentionally thin for future expansion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python -c "import kai_statistics as ks; expected = ['__version__', 'descriptive', 'io', 'profiling', 'testing']; assert ks.__version__ == '0.1.0'; assert ks.__all__ == expected; assert callable(ks._hello); assert all(hasattr(ks, name) for name in expected[1:])"`
- [ ] `uv run python -c "from pathlib import Path; root = Path('src/kai_statistics/__init__.py').read_text(encoding='utf-8'); assert '__version__ = \"0.1.0\"' in root; assert 'from . import descriptive, io, profiling, testing' in root; assert '__all__ = [\"__version__\", \"descriptive\", \"io\", \"profiling\", \"testing\"]' in root; assert 'def _hello(' in root"`
- [ ] `uv run python -c "from pathlib import Path; assert '__all__ = []' in Path('src/kai_statistics/io.py').read_text(encoding='utf-8'); assert '__all__ = []' in Path('src/kai_statistics/profiling.py').read_text(encoding='utf-8'); assert '__all__ = []' in Path('src/kai_statistics/descriptive.py').read_text(encoding='utf-8'); assert '__all__ = []' in Path('src/kai_statistics/testing.py').read_text(encoding='utf-8')"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Package root exposes only the intended Phase 1 module boundaries
- No fake statistical helpers or extra future-domain stubs were introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-library-infrastructure/01-01-SUMMARY.md`
</output>
