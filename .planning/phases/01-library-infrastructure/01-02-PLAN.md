---
phase: 01-library-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/kai_statistics/_core/__init__.py
  - src/kai_statistics/_core/errors.py
  - src/kai_statistics/_core/validation.py
  - src/kai_statistics/_core/results.py
autonomous: true
requirements: [INFRA-02]
user_setup: []
must_haves:
  truths:
    - "Invalid dataframe-like inputs raise `InputValidationError` from the internal `_core` namespace instead of silently coercing values."
    - "Missing columns and `policy=None` raise package-specific validation errors before any helper can continue."
    - "`AnalysisResult` reliably provides metadata and caveat containers for multi-part outputs, and `_core` remains internal-only instead of being re-exported from the package root."
  artifacts:
    - path: "src/kai_statistics/_core/errors.py"
      provides: "Package error hierarchy anchored by KaiStatisticsError"
      exports: ["KaiStatisticsError", "InputValidationError", "MissingValueHandlingError", "ResultContractError"]
      contains: "class KaiStatisticsError"
    - path: "src/kai_statistics/_core/validation.py"
      provides: "Fail-fast validation helpers for dataframe-like inputs"
      exports: ["ensure_columns_present", "ensure_dataframe", "require_explicit_na_policy"]
      contains: "def ensure_dataframe("
    - path: "src/kai_statistics/_core/results.py"
      provides: "Lightweight result and caveat dataclasses"
      exports: ["AnalysisResult", "ResultCaveat"]
      contains: "class AnalysisResult"
    - path: "src/kai_statistics/_core/__init__.py"
      provides: "Internal shared namespace exports"
      contains: "__all__"
  key_links:
    - from: "src/kai_statistics/_core/__init__.py"
      to: "src/kai_statistics/_core/errors.py"
      via: "explicit internal export"
      pattern: "from \\.errors import"
    - from: "src/kai_statistics/_core/__init__.py"
      to: "src/kai_statistics/_core/validation.py"
      via: "explicit internal export"
      pattern: "from \\.validation import"
    - from: "src/kai_statistics/_core/__init__.py"
      to: "src/kai_statistics/_core/results.py"
      via: "explicit internal export"
      pattern: "from \\.results import"
---

<objective>
Define the internal shared conventions that future helpers will reuse for validation, error handling, and multi-part result payloads.

Purpose: Prevent Phase 2 statistical helpers from inventing ad hoc contracts by giving them one internal `_core` namespace with concrete, reusable primitives.
Output: `_core` package with explicit exports for errors, validation helpers, and lightweight result containers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-library-infrastructure/01-CONTEXT.md
@.planning/phases/01-library-infrastructure/01-PATTERNS.md
@AGENTS.md
@src/kai_statistics/__init__.py
</context>

<threat_model>
<trust_boundaries>
- User-provided notebook data crosses into internal validation helpers before any statistical method executes.
- Internal errors and result containers shape how future public helpers will surface invalid input and caveats.
- `_core` is intentionally internal and should not become an accidental public API promise through root-level re-exports.
</trust_boundaries>

<stride_register>
- threat_id: T-01-04
  category: tampering
  component: validation contract
  disposition: mitigate
  mitigation_plan: Implement exact fail-fast helpers for dataframe detection, missing columns, and explicit missing-value policy selection so future helpers share one enforced path.
- threat_id: T-01-05
  category: information_disclosure
  component: error messages
  disposition: mitigate
  mitigation_plan: Raise package-specific exceptions with clear argument and column names while avoiding leaking irrelevant internal stack assumptions into the public API contract.
- threat_id: T-01-06
  category: elevation_of_privilege
  component: internal shared namespace
  disposition: mitigate
  mitigation_plan: Keep `_core` imports explicit and do not re-export internal primitives from `src/kai_statistics/__init__.py` in this phase.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the internal error hierarchy and `_core` export surface</name>
  <files>src/kai_statistics/_core/__init__.py, src/kai_statistics/_core/errors.py</files>
  <read_first>src/kai_statistics/__init__.py, .planning/phases/01-library-infrastructure/01-CONTEXT.md, .planning/phases/01-library-infrastructure/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Create `src/kai_statistics/_core/` as the internal shared namespace. In `src/kai_statistics/_core/errors.py`, define the exact exception classes `KaiStatisticsError`, `InputValidationError`, `MissingValueHandlingError`, and `ResultContractError`, keeping them small and type-hinted. In `src/kai_statistics/_core/__init__.py`, export those classes with an explicit `__all__`. Do not re-export `_core` symbols from the package root and do not introduce public helper functions here.</action>
  <verify>uv run python -c "from kai_statistics._core import InputValidationError, KaiStatisticsError, MissingValueHandlingError, ResultContractError; print([c.__name__ for c in (KaiStatisticsError, InputValidationError, MissingValueHandlingError, ResultContractError)])"</verify>
  <acceptance_criteria>
    - `src/kai_statistics/_core/errors.py` contains `class KaiStatisticsError`
    - `src/kai_statistics/_core/errors.py` contains `class InputValidationError`
    - `src/kai_statistics/_core/errors.py` contains `class MissingValueHandlingError`
    - `src/kai_statistics/_core/errors.py` contains `class ResultContractError`
    - `src/kai_statistics/_core/__init__.py` contains `__all__ = [`
  </acceptance_criteria>
  <done>The `_core` package exports the shared error hierarchy without changing the public package root surface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-fast validation helpers for dataframe inputs</name>
  <files>src/kai_statistics/_core/validation.py, src/kai_statistics/_core/__init__.py</files>
  <read_first>src/kai_statistics/_core/__init__.py, .planning/phases/01-library-infrastructure/01-CONTEXT.md, .planning/phases/01-library-infrastructure/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Create `src/kai_statistics/_core/validation.py` with the exact helper functions `ensure_dataframe(data, arg_name="data")`, `ensure_columns_present(dataframe, columns, arg_name="data")`, and `require_explicit_na_policy(policy, allowed=("drop", "keep", "error"))`. `ensure_dataframe` must raise `InputValidationError` when the input is not a pandas `DataFrame`; `ensure_columns_present` must raise `InputValidationError` listing the missing column names; `require_explicit_na_policy` must raise `MissingValueHandlingError` when `policy` is `None` or not in the allowed tuple. Export these helpers from `_core/__init__.py`.</action>
  <verify>uv run python -c "import pandas as pd; from kai_statistics._core import InputValidationError, MissingValueHandlingError, ensure_columns_present, ensure_dataframe, require_explicit_na_policy; df = ensure_dataframe(pd.DataFrame({'x':[1]})); ensure_columns_present(df, ['x']); assert require_explicit_na_policy('keep', ('drop','keep','error')) == 'keep'; failures = 0; scenarios = ((lambda: ensure_dataframe([]), InputValidationError), (lambda: ensure_columns_present(pd.DataFrame({'x':[1]}), ['y']), InputValidationError), (lambda: require_explicit_na_policy(None, ('drop','keep','error')), MissingValueHandlingError));\nfor fn, exc in scenarios:\n    try:\n        fn()\n    except exc:\n        failures += 1\nassert failures == 3"</verify>
  <acceptance_criteria>
    - `src/kai_statistics/_core/validation.py` contains `def ensure_dataframe(`
    - `src/kai_statistics/_core/validation.py` contains `def ensure_columns_present(`
    - `src/kai_statistics/_core/validation.py` contains `def require_explicit_na_policy(`
    - `src/kai_statistics/_core/validation.py` contains `MissingValueHandlingError`
    - `src/kai_statistics/_core/validation.py` contains `raise InputValidationError`
    - `src/kai_statistics/_core/validation.py` contains `raise MissingValueHandlingError`
  </acceptance_criteria>
  <done>The shared validation layer enforces fail-fast dataframe and missing-policy rules for future helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Add lightweight result containers for metadata and caveats</name>
  <files>src/kai_statistics/_core/results.py, src/kai_statistics/_core/__init__.py</files>
  <read_first>src/kai_statistics/_core/__init__.py, .planning/phases/01-library-infrastructure/01-CONTEXT.md, .planning/phases/01-library-infrastructure/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Create `src/kai_statistics/_core/results.py` with `@dataclass` definitions for `ResultCaveat` and `AnalysisResult`. `ResultCaveat` must expose the exact fields `code`, `message`, and `severity`. `AnalysisResult` must expose the exact fields `value`, `metadata`, and `caveats`, with `metadata` defaulting to an empty dict and `caveats` defaulting to an empty list through dataclass factories. Export both symbols from `_core/__init__.py`. Keep the container lightweight; do not build a full report framework in this phase.</action>
  <verify>uv run python -c "from kai_statistics._core import AnalysisResult, ResultCaveat; empty_result = AnalysisResult(value='ok'); seeded_result = AnalysisResult(value={'ok': True}, metadata={'source': 'test'}, caveats=[ResultCaveat(code='small-sample', message='n<30', severity='warning')]); assert isinstance(empty_result.metadata, dict); assert empty_result.metadata == {}; assert isinstance(empty_result.caveats, list); assert empty_result.caveats == []; assert seeded_result.caveats[0].code == 'small-sample'"</verify>
  <acceptance_criteria>
    - `src/kai_statistics/_core/results.py` contains `@dataclass`
    - `src/kai_statistics/_core/results.py` contains `class ResultCaveat`
    - `src/kai_statistics/_core/results.py` contains `class AnalysisResult`
    - `src/kai_statistics/_core/results.py` contains `metadata:`
    - `src/kai_statistics/_core/results.py` contains `caveats:`
    - `src/kai_statistics/_core/results.py` contains `field(default_factory=dict)`
    - `src/kai_statistics/_core/results.py` contains `field(default_factory=list)`
  </acceptance_criteria>
  <done>Future helpers have one lightweight internal container for metadata-rich outputs and caveat reporting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python -c "from kai_statistics._core import AnalysisResult, InputValidationError, KaiStatisticsError, MissingValueHandlingError, ResultCaveat, ResultContractError, ensure_columns_present, ensure_dataframe, require_explicit_na_policy; print('core-ready')"`
- [ ] `uv run python -c "from kai_statistics._core import AnalysisResult, InputValidationError, KaiStatisticsError, MissingValueHandlingError, ResultCaveat, ResultContractError, ensure_columns_present, ensure_dataframe, require_explicit_na_policy; assert KaiStatisticsError.__name__ == 'KaiStatisticsError'; assert ResultContractError.__name__ == 'ResultContractError'; assert callable(ensure_dataframe); assert callable(require_explicit_na_policy)"`
- [ ] `uv run python -c "import pandas as pd; from kai_statistics._core import InputValidationError, MissingValueHandlingError, ensure_columns_present, ensure_dataframe, require_explicit_na_policy; failures = 0; checks = ((lambda: ensure_dataframe([]), InputValidationError), (lambda: ensure_columns_present(pd.DataFrame({'x':[1]}), ['y']), InputValidationError), (lambda: require_explicit_na_policy(None, ('drop','keep','error')), MissingValueHandlingError));\nfor fn, exc in checks:\n    try:\n        fn()\n    except exc:\n        failures += 1\nassert failures == 3"`
- [ ] `uv run python -c "from kai_statistics._core import AnalysisResult; result = AnalysisResult(value='ok'); assert isinstance(result.metadata, dict); assert result.metadata == {}; assert isinstance(result.caveats, list); assert result.caveats == []"`
- [ ] `rg --line-number "KaiStatisticsError|InputValidationError|MissingValueHandlingError|ResultContractError|ResultCaveat|AnalysisResult" src/kai_statistics/_core`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `_core` remains an internal namespace with explicit exports only
- Shared validation, errors, and result containers exist with the exact names specified in this plan

</success_criteria>

<output>
After completion, create `.planning/phases/01-library-infrastructure/01-02-SUMMARY.md`
</output>
